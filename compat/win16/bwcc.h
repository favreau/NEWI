#ifndef NEWI_COMPAT_BWCC_H
#define NEWI_COMPAT_BWCC_H

// Borland Windows Custom Controls. The engine includes it only to pull in the
// basic Windows types (POINT, RECT, HINSTANCE); no dialog controls are used.

#include <windows.h>

#endif // NEWI_COMPAT_BWCC_H
